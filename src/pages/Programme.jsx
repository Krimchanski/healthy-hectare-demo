import React from "react";
import PageShell from "../components/layout/PageShell";

export default function Programme() {
    return (
        <PageShell
            title="Programme"
            subtitle="Dedicated programme detail page (demo placeholder). This will later hold participation tracks, governance documents, timelines, and field protocol downloads in a structured format."
            maxWidthClass="max-w-7xl"
        >
            {/* Page content goes here. For now, the header carries the placeholder copy. */}
        </PageShell>
    );
}
